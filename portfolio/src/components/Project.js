import React, { useState, useContext} from 'react';
//we'll have to use a github api call to dynamically get my github
//repositories
function Project(props) {
    //set this up to be a card make api call on page?
    return (
        <>
        </>
    );
}

export default Project;